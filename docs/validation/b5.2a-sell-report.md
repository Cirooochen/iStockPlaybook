# B.5.2A — SELL Transaction Validation Report

**Status:** COMPLETE
**Date:** 2026-09-08
**Scope:** SELL only (per `docs/phase-b5-algorithm-validation.md` B.5.2). BUY, B.5.2B, and later B.5 stages not tested.
**Method:** Real transaction path — `applySell()` (the same function `PlaybookClientShell.handleTransaction` calls) followed by `runDecisionEngine()` on the resulting state. Not hand-constructed.
**Rule:** No rule/model issues fixed automatically.

---

## 1. Pre-transaction state

Unity baseline (unchanged from B.5.1): 902 shares, avg cost €27.76, price €40.46, portfolio total €62,280, target weight range 40–45%, core range 600–650, thesis INTACT. Full pre-transaction engine output already validated in `docs/validation/b5.1-baseline-report.md` (concentration SEVERELY_OVERWEIGHT, stance HOLD_GRADUALLY_TRIM, `targetPosition.trimCapacity` = {minimum: 210, preferred: 252, maximumNormal: 286}).

## 2. Exact SELL input

`applySell(unitySeed.position, { shares: 100, priceEur: 40.46 }, currentPriceEur: 40.46, oldTotalPortfolioEur: 62280)` — sell at the current market price, no discount/premium. Price, thesis, fundamentals, valuation, and momentum held constant per instruction.

## 3. Post-transaction position math

| Field | Value | Formula |
|---|---|---|
| Shares | 802 | 902 − 100 |
| Average cost | €27.76 | unchanged — weighted-average SELL convention, not tax-lot |
| Position value | €32,448.92 | 802 × €40.46 |
| Unrealized return | 45.75% | unchanged — same price, same avg cost |
| Realized gain | €1,270.00 (+45.75%) | (40.46 − 27.76) × 100 |

## 4. Post-transaction portfolio weight

| Field | Value |
|---|---|
| Portfolio total | **€62,280 — unchanged** |
| Unity weight | **52.1%** (down from 58.6%) |

Portfolio total preservation verified through the real `applySell`/`calcPortfolioTotalAfterSell` path, not asserted in isolation — this is the first real-transaction confirmation that the B.5.1 Resolution's portfolio-total fix actually holds under propagation, not just in its own unit tests.

## 5. Engine outputs (full propagation, real `runDecisionEngine()` call)

| Layer | Pre-SELL | Post-SELL |
|---|---|---|
| `concentration.state` | SEVERELY_OVERWEIGHT | **OVERWEIGHT** |
| `concentration.targetShares` | 692 | 692 (unchanged — depends only on total/price/target%) |
| `concentration.sharesToTarget` | 210 | 110 |
| `concentration.tacticalInventory` | {aboveCoreMax: 252, maxSellToCore: 302} | {aboveCoreMax: 152, maxSellToCore: 202} |
| `concentration.trimSizing` | {level1: 76, level2: 88, level3: 88, maxTacticalTrim: 252} | {level1: 46, level2: 53, level3: 53, maxTacticalTrim: 152} |
| `thesis.health` | INTACT | INTACT (unchanged, as instructed) |
| `constraints.hc001.triggered` | true (58.6% > 50%) | true (52.1% > 50%) |
| `constraints.hc002.triggered` | false | false |
| `constraints.accumulationEnabled` | false | false |
| `stance` | HOLD_GRADUALLY_TRIM | **HOLD_TRIM** |
| `actionZones.ADD` | INACTIVE | INACTIVE |
| `actionZones.HOLD` | ACTIVE | ACTIVE |
| `actionZones.TRIM_1` | ACTIVE | **WATCH** |
| `actionZones.TRIM_2` | WATCH | **INACTIVE** |
| `actionZones.THESIS_REVIEW` | CONDITIONAL | CONDITIONAL |
| `scorecard.positionFit` | {4, Neutral} | {7, Positive} |
| `scorecard.concentrationRisk` | {2, Elevated} | {3, Elevated} |
| `scorecard.fundamentals/valuation/momentum` | unchanged | unchanged (as instructed) |
| `targetPosition.positionSizingState` | OVERWEIGHT | OVERWEIGHT (unchanged — both pre/post are above the target range; this 3-state model has no severity granularity, unlike `concentration.state`'s 4 tiers — expected, not a bug) |
| `targetPosition.weightShareRange` | [616, 692] | [616, 692] (unaffected by this position's size) |
| `targetPosition.feasibleStrategyRange` | [616, 650] | [616, 650] |
| `targetPosition.trimCapacity` | {minimum: 210, preferred: 252, maximumNormal: 286} | **{minimum: 110, preferred: 152, maximumNormal: 186}** |

**Rules triggered:** `HC-001` only, both before and after (weight still exceeds the 50% short-term ceiling). `HC-002` not triggered (thesis not BROKEN). **HC-003** — checked separately (`checkHC003(100, 902, 600, "INTACT")`, transaction-time only, not part of `EngineOutput`): remaining shares (802) stay well above the 600-share core minimum, so **not triggered** — this specific 100-share sell does not breach the core.

## 6. Expected vs. actual

| Expectation | Actual | Match |
|---|---|---|
| Shares 902 → 802 | 802 | ✓ |
| Portfolio total preserved | €62,280 unchanged | ✓ |
| Portfolio weight reduced materially | 58.6% → 52.1% | ✓ |
| Tactical inventory reduced | 252 → 152 | ✓ |
| Remaining trim capacity reduced | all three tiers down by exactly 100 (210→110, 252→152, 286→186) | ✓ |
| Thesis unchanged | INTACT → INTACT | ✓ |
| ADD not automatically enabled unless HC clears | HC-001 still triggered (52.1% > 50%); ADD stays INACTIVE | ✓ — correctly did NOT clear |
| Core preserved | remaining 802 shares ≫ 600-share core min; HC-003 not triggered | ✓ |
| Final stance — not forced | **HOLD_TRIM** (de-escalated from HOLD_GRADUALLY_TRIM) | Reported as-is — this is the actual output of the approved stance matrix (§18: OVERWEIGHT + non-weakening thesis → HOLD_TRIM), not an assumption. |

No discrepancy between expected direction and actual result was found in this scenario.

## 7. PASS / FAIL by layer

| Layer | Result |
|---|---|
| Position | PASS |
| Portfolio | PASS |
| Concentration | PASS |
| Target Position | PASS |
| Constraints | PASS |
| Stance | PASS |
| Action Zones | PASS |
| Existing `trimSizing` (reported separately) | PASS (internally consistent) — see standing design question below |

**All layers PASS.** No new implementation bugs found in this scenario.

## 8. Bug vs. model-design distinction

No new findings. One **standing, already-classified** item resurfaces with new numbers, not a new issue:

- `concentration.trimSizing.maxTacticalTrim` (152) still numerically equals `targetPosition.trimCapacity.preferred` (152) post-transaction, same as pre-transaction (252 = 252). This is the same **RULE / MODEL DESIGN QUESTION** already recorded in the B.5.1 Resolution closeout (`concentration.trimSizing` computes the "Preferred" tier only, with no Minimum/Maximum exposed) — confirmed to persist under a real transaction, not re-litigated or reclassified here.

## 9. Tests / results

**Added:** `src/domain/validation/b5.2a-sell.test.ts` — 1 test. Calls `applySell()` (real transaction path) then `runDecisionEngine()` (real engine) on the result — does not hand-construct the post-sell state. Asserts position math, portfolio-total preservation, concentration/stance/action-zone transitions, constraints, and both `trimSizing` and `targetPosition.trimCapacity` reported side by side (not equated).

**Run results:**
- `npx vitest run` — **66/66 passing** (65 pre-existing + 1 new)
- `npx tsc --noEmit` — clean
- `npx eslint` (new file) — clean
- `npm run build` — succeeds

## 10. Open questions

1. Should `concentration.trimSizing` eventually be reconciled to consume `targetPosition.trimCapacity` (already tracked as a TODO in the B.5.1 Resolution closeout)? Not resolved here — explicitly out of scope for B.5.2A.
2. `targetPosition.positionSizingState` has no severity granularity (unlike `concentration.state`'s 4 tiers) — this SELL didn't change it (OVERWEIGHT before and after), which is correct behavior but worth keeping in mind for B.5.3 (Concentration Threshold Validation), where the two models' differing granularity will matter more.

## 11. Final B.5.2A verdict

**PASS.** SELL of 100 Unity shares propagates correctly and consistently through every layer of the current architecture — position accounting, portfolio-total preservation, concentration re-classification, the new target-position capacity model, hard constraints, stance, action zones, and the scorecard — with no implementation bugs found and no rule/model questions newly introduced. The one standing design question (`trimSizing` vs. `trimCapacity`) is unchanged from its prior classification. B.5.2A is complete; B.5.2B (or later stages) not started.
